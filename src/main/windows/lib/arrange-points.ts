import type { BlockProps } from 'shared/types'
export function arrangePoints(
  block: BlockProps,
  reading: Array<boolean | number>
) {
  console.log(block)
  console.log('------')
  console.log(reading)
}
